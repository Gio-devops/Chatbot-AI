name: Python Application CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: 1. Checkout do código
      uses: actions/checkout@v4

    - name: 2. Configurar ambiente Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: 3. Instalar dependências
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: 4. Análise de código com Ruff (Lint)
      run: |
        ruff check .

    - name: 5. Executar testes com Pytest
      run: |
        # Precisamos simular a API Key para os testes, mesmo que não seja usada
        export GEMINI_API_KEY="fake-key-for-testing"
        pytest

    - name: 6. Simulação de Build da imagem Docker
      run: |
        docker build . --tag pyunit-scribe:latest

    - name: 7. Simulação de Deploy
      run: |
        echo "✅ Pipeline concluído com sucesso!"
        echo "🚀 Simulando o deploy da aplicação..."
        # Em um cenário real, aqui entrariam comandos para publicar 
        # a imagem Docker e atualizar o serviço na nuvem (ex: AWS ECS, GCP Cloud Run)
        echo "🚀 Deploy simulado finalizado."